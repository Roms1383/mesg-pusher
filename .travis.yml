language: node_js
# node version is set by .nvmrc
cache:
  yarn: true
  directories:
    - node_modules
git:
  submodules: false
before_install:
  - echo "updating Yarn..."
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.9.4
  - export PATH="$HOME/.yarn/bin:$PATH"
  - echo "installing dependencies..."
  - yarn install
script:
  - yarn test || travis_terminate 1;
stages:
  - stage: test
    name: "Unit-tests"
  - stage: release
    name: "Semantic release"
    if: branch = release
jobs:
  include:
    - stage: test
    - stage: release
      install: true
      after_success:
        - yarn semantic-release --branch $TRAVIS_BRANCH